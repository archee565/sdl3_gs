#version 450

layout(set = 1, binding = 0, std430) buffer OutputBuffer {
    uint data[];
} output_buf;

layout(local_size_x = 64, local_size_y = 1, local_size_z = 1) in;

void main() {
    uint idx = gl_GlobalInvocationID.x;
    if (idx < 256) {
        output_buf.data[idx] = idx;
    }
}
